#!/bin/bash

ansible-pull -o --check -U https://github.com/eedorenko/kalypso-oci-gitops -C dev -d control-plane  > checklog

if cat checklog | grep \"changed\" | grep -q false
then
  exit
fi

echo "changed"